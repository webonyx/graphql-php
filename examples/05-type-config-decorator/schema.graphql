type Query {
  tracksForHome: [Track!]!
}

type Track {
  id: ID!
  title: String
  thumbnail: String
  length: Int
  modulesCount: Int
  author: Author
}

type Author {
  id: ID!
  name: String
  photo: String
}
